language: ruby
bundler_args: --without development
rvm:
  - 1.9.3
  - 2.2.2
  - ruby-head
  - jruby
  - jruby-head
  - rbx-2

# jdk:
#   - oraclejdk7
#   - openjdk7

env: CI="travis"

matrix:
  allow_failures:
    - rvm: 1.9.3
    - rvm: jruby
    - rvm: ruby-head

services:
  - mongodb

script: "bundle exec rspec"

sudo: false